name = "mercatone-della-frutta"
pages_build_output_dir = "dist/gestionale-pwa/browser"

[[d1_databases]]
binding = "DB"
database_name = "mercatone-della-frutta"
database_id = "fcf01dbf-fb6c-4723-992c-00e25ca88274"

# ---------------------------------------------------------------------------
# Required secrets — set these in the Cloudflare Pages dashboard under
# Settings → Environment variables (mark them as "Secret"):
#
#   PHONE_NUMBER_1   Recipient phone number (e.g. +393471234567)
#   TEXTBELT_URL     (optional) Textbelt endpoint
#                    (defaults to https://textbelt.com/text)
#   TEXTBELT_KEY     (optional) API key
#                    (defaults to the free "textbelt" key)
# ---------------------------------------------------------------------------
